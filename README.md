# AssignmentLesson4
Assignment: Getting and Cleaning Data Course Project

The script run_analysis.R included a function called processData().
To run this sunction, ensure that is is in the same directory 
holding the "UCI HAR Dataset" folder. 
The assignment result will be written to a file called "step4out.txt"

